{
  "name": "nylas-pricing-functions",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild QuoteOptionsFunction.js --bundle --platform=node --target=node20 --format=cjs --external:@hubspot/api-client --outfile=QuoteOptionsFunction.bundle.js",
    "test": "node --test *.test.js"
  },
  "dependencies": {
    "@hubspot/api-client": "^14.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.8"
  }
}
